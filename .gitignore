*.klib
.kotlin/
*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
*.apk
*.ap_
*.aab
*.dex
*.class
bin/
gen/
Thumbs.db
.classpath
.project
.idea/
.gradle/
build/
app/build/
keystore/
obj/
*.jks
*.so
libavcodec.so
libavdevice.so
libavfilter.so
libavformat.so
libavutil.so
libc++_shared.so
libmpv.so
libplayer.so
libpostproc.so
libswresample.so
lbswscale.so
splpingsrccode/
.claude/

# Compiled native output belongs in build/, never in src/
shared/src/jvmMain/resources/native/

# The Kotlin framework is embedded directly by a Gradle build phase in the
# Xcode project, so CocoaPods plays no part. These paths stay ignored in case a
# stray `pod install` puts them back.
iosApp/Pods/
iosApp/Podfile.lock
iosApp/iosApp.xcworkspace/
shared/shared.podspec

# Legal texts are copied into the APK from the repository root at build time.
androidApp/src/main/assets/legal/
