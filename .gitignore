*.klib
.kotlin/
*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
*.apk
*.ap_
*.aab
*.dex
*.class
bin/
gen/
Thumbs.db
.classpath
.project
.idea/
.gradle/
build/
app/build/
keystore/
obj/
*.jks
*.so
libavcodec.so
libavdevice.so
libavfilter.so
libavformat.so
libavutil.so
libc++_shared.so
libmpv.so
libplayer.so
libpostproc.so
libswresample.so
lbswscale.so
splpingsrccode/
.claude/

# Compiled native output belongs in build/, never in src/
shared/src/jvmMain/resources/native/

# CocoaPods is no longer used; the Kotlin framework is embedded directly by the
# Gradle build phase in the Xcode project. These paths stay ignored so a stray
# `pod install` cannot reintroduce them.
iosApp/Pods/
iosApp/Podfile.lock
iosApp/iosApp.xcworkspace/
shared/shared.podspec

# Legal texts are copied into the APK from the repository root at build time.
androidApp/src/main/assets/legal/
